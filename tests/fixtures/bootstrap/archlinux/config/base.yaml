features:
  core:
    pacman:
      - base
      - git
    aur:
      - yay-bin
    services:
      - resolved.service
    user_services:
      - pipewire.socket
  ci:
    pacman:
      - age
      - base-devel
      - gnupg
      - python
      - python-docutils
      - rsync
      - sudo
    aur:
      - git-remote-gcrypt
  steam:
    pacman:
      - steam
    multilib: true
  printing:
    pacman:
      - cups
    services:
      - cups.service
multilib:
  - lib32-mesa
