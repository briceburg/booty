#!/usr/bin/env bash
set -euo pipefail

booty_bin="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)/booty"
export BOOTY_SECRETS_DIR="${BOOTY_SECRETS_DIR:-$HOME/git/booty-secrets}"

exec "$booty_bin" "$@"
